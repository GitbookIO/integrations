{
    "extends": "@gitbook/tsconfig/integration.json",
    "compilerOptions": {
        "strict": true,
        "lib": ["ESNext", "DOM"],
        "downlevelIteration": true,
        "types": [
            "bun-types" // add Bun global
        ]
    }
}
