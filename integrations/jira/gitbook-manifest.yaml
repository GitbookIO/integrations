name: jira
title: Jira
description: Embed Jira issue directly in your GitBook documentation.
script: src/index.tsx
scopes: []
blocks:
    - id: embed
      title: Jira
      description: Information about a Jira task
      urlUnfurl:
          - https://gitbook-dev-steven.atlassian.net/browse
configurations:
    account:
        properties:
            oauth_credentials:
                type: button
                title: Connection
                description: Authorization between Jira and GitBook.
                button_text: Authorize
                callback_url: /oauth
        required:
            - oauth_credentials
secrets:
    CLIENT_ID: ${{ env.JIRA_CLIENT_ID }}
    CLIENT_SECRET: ${{ env.JIRA_CLIENT_SECRET }}
