export class TextEncoder {
    public encode(input) {
        // TODO
        return input;
    }
}

export class TextDecoder {
    public encoding: any;
    constructor(encoding) {
        this.encoding = encoding;
    }
    public decode(input) {
        // TODO
        return input;
    }
}
